#!/bin/bash

- name: error handling
  hosts: local 
  connection: local
  tasks:
  - name: checking the roboshop user exist or not
    ansible.builtin.command: id roboshop
    ignore_errors: yes
    register: output

    