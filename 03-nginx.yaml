#!/bin/bash

- name: install the nginx
  hosts: worker-node
  become: yes #this means giving sudo access
  tasks: 
  - name: install the nginx
    ansible.builtin.package:
      name: nginx
      state: installed

  - name: start and enabled the nginx
    ansible.builtin.service:
      name: nginx
      state: started  
      enabled: yes